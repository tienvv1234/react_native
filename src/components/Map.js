import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const Map = ({params}) => {
    return (
        <View>
            <Text>Map</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default Map;
